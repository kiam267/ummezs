import { authClient } from '@/lib/auth-client';
import Image from 'next/image';

export default async function Home() {
  // const { } = await authClient()
  return (
    <main>
      <h1>Welcome to the Home Page</h1>

    </main>
  );
}
